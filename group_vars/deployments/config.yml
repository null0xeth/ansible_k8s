---
# GROUP: DEPLOYMENTS
# All variables inside this file can be changed to
# Match your specific needs and requirements
# Changes made to these variables will be automatically
# processed in `config_derived.yml`
deployment_file_dir: "{{ global_home_dir }}/deployments"

metallb_version: "v0.14.4"
metallb_pool_name: "pool01"
metallb_pool_ip_range: "192.168.105.180-192.168.105.190"
metallb_pool_adv_name: "pushpool01"

longhorn_version: "v1.6.1"
certmanager_version: "v1.14.4"
longhorn_check_swag: /home/null0x/checklonghorn.sh
helm_version: "v3.15.3"
krew_version: "v0.4.4"
krew_binary: "{{ global_home_dir }}/.krew/bin/kubectl-krew"
krew_bin_dir: "{{ global_home_dir }}/.krew/bin/"
