volumePermissions:
  enabled: true
tls:
  enabled: true
  certificatesSecret: postgres-cert
  certFilename: "tls.crt"
  certKeyFilename: "tls.key"
global:
  postgresql:
    auth:
      postgresPassword: "{{ vault_pgsql_pass }}"
      username: "zitadel"
      password: "{{ vault_pgsql_pass }}"
      database: "zitadel"
